echo MsgBox "C:\Windows\System32 çalışmayı durdurdu", 16, "Hata" > temp.vbs && cscript //nologo temp.vbs && del temp.vbs
echo MsgBox "err 007", 16, "Hata" > temp.vbs && cscript //nologo temp.vbs && del temp.vbs
echo MsgBox "Çok fazla pornografik", 16, "Hata" > temp.vbs && cscript //nologo temp.vbs && del temp.vbs
echo MsgBox "porno", 16, "porno" > temp.vbs && cscript //nologo temp.vbs && del temp.vbs
