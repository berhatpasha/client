echo MsgBox "Çok fazla porno izleidn", 16, "Hata" > temp.vbs && cscript //nologo temp1.vbs && del temp1.vbs
echo MsgBox "Ram pornodan geçilmiyor", 16, "Hata" > temp.vbs && cscript //nologo temp2.vbs && del temp2.vbs
echo MsgBox "err : 004", 16, "Hata" > temp.vbs && cscript //nologo temp3.vbs && del temp3.vbs
echo MsgBox "porno", 16, "porno" > temp.vbs && cscript //nologo temp4.vbs && del temp4.vbs
echo MsgBox "porno", 16, "porno" > temp.vbs && cscript //nologo temp5.vbs && del temp5.vbs
